#!/usr/bin/env bash

echo
MW=$1 java -jar target/vertx-web-benchmark-3.0.0-fat.jar 2> /dev/null &
pid=$!

sleep 2

wrk 'http://localhost:3333/?foo[bar]=baz' \
  -d 3 \
  -c 50 \
  -t 8 \
  | grep 'Requests/sec' \
  | awk '{ print "  " $2 }'

kill $pid
