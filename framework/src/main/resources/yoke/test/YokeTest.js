
function YokeTest() {
    // (Vertx vertx, GYoke yoke, boolean fakeSSL)
    // super(vertx, yoke.toJavaYoke(), fakeSSL);
    // (Vertx vertx, GYoke yoke)
    // this(vertx, yoke, false);
}

YokeTester.prototype.request = function() {
    var args = Array.prototype.slice(arguments);
    // (final String method, final String url, final Closure<Response> handler)
    // (final String method, final String url, final Map<String, Object> headers, final Closure<Response> handler)
    // (final String method, final String url, final Map<String, Object> headers, final Buffer body, final Closure<Response> handler)
};